---
interface Props {
  title: string;
  rightsLabel: string;
}

const { title, rightsLabel } = Astro.props;
const currentYear = new Date().getFullYear();
---

<footer class="site-footer">
  <div class="container">
    <div class="footer-content">
      <div class="logo">{title}</div>
      <p class="footer-text">© {currentYear} {rightsLabel}</p>
    </div>
  </div>
</footer>

